Pixel-Interpreter-Framework
===========================

Takes a string and determines what command to send and where to send it. The
modules folder contains different command classes that inherit from the base
command class.
